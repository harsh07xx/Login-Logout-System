{
  "name": "login-app-backend",
  "version": "1.0.0",
  "description": "Static file server + API proxy for the login demo app",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
